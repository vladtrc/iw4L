[package]
name = "fastfile_iw5"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Retail IW5 / MW3 zone container: header, stream, pointer fixup → asset_iw4 IR."

[dependencies]

[lints]
workspace = true
