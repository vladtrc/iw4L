[package]
name = "frame"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared frame TOC labels and session identity. No plugins, no systems."

[dependencies]
bevy.workspace = true

[lints]
workspace = true
