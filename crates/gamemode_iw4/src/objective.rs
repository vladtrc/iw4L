#[derive(Clone, Copy, Debug, Default, PartialEq)]
pub struct Objective {
    pub state: i32,
    pub origin_0: f32,
    pub origin_1: f32,
    pub origin_2: f32,
    pub ent_num: i32,
    pub team_num: i32,
    pub icon: i32,
}
