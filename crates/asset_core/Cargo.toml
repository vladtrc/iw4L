[package]
name = "asset_core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Generic asset identity: AssetKey, AssetRef, IndexSpace, AssetEdge, ZoneGame."

[dependencies]

[lints]
workspace = true
