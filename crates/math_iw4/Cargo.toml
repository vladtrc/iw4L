[package]
name = "math_iw4"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Retail IW4 numeric conventions where bit-exactness matters: angle packing, normalization, Euler order."

[dependencies]
libm = { version = "0.2.16", default-features = false }

[lints]
workspace = true
