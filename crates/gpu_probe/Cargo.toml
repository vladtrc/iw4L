[package]
name = "gpu_probe"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Process-global slots a patched wgpu writes into and the frame recorder reads."

[dependencies]

[lints]
workspace = true
